# NPO予算管理システム「ながいく」- 共通設定
# このファイルでIPアドレスとポート設定を一元管理します

# サーバーIPアドレス（変更時はここだけ修正）
SERVER_IP=160.251.170.97

# 本番環境設定
PROD_FRONTEND_PORT=3000
PROD_BACKEND_PORT=8000

# 開発環境設定  
DEV_FRONTEND_PORT=3001
DEV_BACKEND_PORT=8001

# 構築されるURL（自動生成用）
PROD_FRONTEND_URL=http://${SERVER_IP}:${PROD_FRONTEND_PORT}
PROD_BACKEND_URL=http://${SERVER_IP}:${PROD_BACKEND_PORT}
DEV_FRONTEND_URL=http://${SERVER_IP}:${DEV_FRONTEND_PORT}
DEV_BACKEND_URL=http://${SERVER_IP}:${DEV_BACKEND_PORT}

# データベース設定
DATABASE_USER=nagaiku_user
DATABASE_PASSWORD=nagaiku_password2024
DATABASE_HOST=localhost
DATABASE_PORT=5432
PROD_DATABASE_NAME=nagaiku_budget
DEV_DATABASE_NAME=nagaiku_budget_dev 